class Solution {
public:
    void moveZeroes(vector<int>& nums) {
        int pos = 0;
        for(int iter = 0;iter<nums.size();iter++)
        {
            if(nums[iter] !=0)
            {
                swap(nums[iter],nums[pos++]);
            }
        }
    }
};
